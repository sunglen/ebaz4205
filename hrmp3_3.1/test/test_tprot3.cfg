/*
 *		時間パーティショニングに関するテスト(3)のシステムコンフィギュ
 *		レーションファイル
 *
 *  $Id: test_tprot3.cfg 444 2019-10-04 07:00:41Z ertl-hiro $
 */
INCLUDE("test_common1.cfg");

#include "test_common.h"

DEF_SCY({ 20 * TEST_TIME_CP });
CRE_SOM(SOM1, { TA_INISOM });
CRE_SOM(SOM2, { TA_NULL, SOM3 });
CRE_SOM(SOM3, { TA_NULL, SOM4 });
CRE_SOM(SOM4, { TA_NULL, SOM2 });

CLASS(CLS_PRC1) {
  ATT_TWD({ DOM2, SOM1, 1, 5 * TEST_TIME_CP });
  ATT_TWD({ DOM1, SOM1, 2, 5 * TEST_TIME_CP });

  ATT_TWD({ DOM1, SOM2, 1, 5 * TEST_TIME_CP });
  ATT_TWD({ DOM2, SOM2, 2, 5 * TEST_TIME_CP });

  ATT_TWD({ DOM1, SOM3, 1, 10 * TEST_TIME_CP });

  DOMAIN(DOM1) {
	ACV_DOM({ TACP_SHARED, TACP_SHARED, TACP_KERNEL, TACP_SHARED });
	CRE_TSK(TASK1, { TA_ACT, 1, task1, MID_PRIORITY, STACK_SIZE, NULL });
	SAC_TSK(TASK1, { TACP_SHARED, TACP_SHARED, TACP_KERNEL, TACP_SHARED });
  }
  DOMAIN(DOM2) {
	ACV_DOM({ TACP_SHARED, TACP_SHARED, TACP_KERNEL, TACP_SHARED });
	CRE_TSK(TASK2, { TA_ACT, 1, task2, MID_PRIORITY, STACK_SIZE, NULL });
	SAC_TSK(TASK2, { TACP_SHARED, TACP_SHARED, TACP_KERNEL, TACP_SHARED });
  }
  DOMAIN(DOM3) {
	ACV_DOM({ TACP_SHARED, TACP_SHARED, TACP_KERNEL, TACP_SHARED });
	CRE_TSK(TASK3, { TA_ACT, 1, task3, MID_PRIORITY, STACK_SIZE, NULL });
	SAC_TSK(TASK3, { TACP_SHARED, TACP_SHARED, TACP_KERNEL, TACP_SHARED });
  }
  KERNEL_DOMAIN {
	SAC_SYS({ TACP_SHARED, TACP_KERNEL, TACP_KERNEL, TACP_SHARED });
	CRE_TSK(TASK4, { TA_ACT, 1, task4, LOW_PRIORITY, STACK_SIZE, NULL });
	SAC_TSK(TASK4, { TACP_SHARED, TACP_SHARED, TACP_KERNEL, TACP_SHARED });
	CRE_CYC(CYC1, { TA_STA, { TNFY_HANDLER, 0, cyclic1_handler },
									20 * TEST_TIME_CP, 20 * TEST_TIME_CP });
	SAC_CYC(CYC1, { TACP_SHARED, TACP_SHARED, TACP_KERNEL, TACP_SHARED });
  }
}
ATT_MOD("test_tprot3.o");

INCLUDE("test_common2.cfg");
